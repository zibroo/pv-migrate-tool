name: Popeye Workflow

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Select the environment.'
        required: true
        default: 'load'
        type: choice
        options:
        - dev
        - qa
        - load
        - staging
      # namespace:


permissions:
  id-token: write
  contents: read

jobs:
  terragrunt:
    runs-on: ubuntu-latest #runner
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Configure AWS credentials from AWS account
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: "us-east-1"

      - name : Connection to Kubernetes Cluster
        run: |
          aws eks --region us-east-1 update-kubeconfig --name prod-eks-cluster
      - name: PV migrate  Setup
        run: |
            wget https://github.com/utkuozdemir/pv-migrate/releases/download/v1.7.1/pv-migrate_v1.7.1_darwin_x86_64.tar.gz
            tar -xvzf pv-migrate_v1.7.1_darwin_x86_64.tar.gz
            mv pv-migrate /usr/local/bin
            pv-migrate --help